<div class="row bg-title">
    <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
        <h4 class="page-title">{{ title }}</h4> </div>
</div>

<div class="row">
    <!-- Left sidebar -->
    <div class="col-md-12">
        <div class="white-box">
            <div class="row">
                {{!-- <div class="col-lg-2 col-md-3  col-sm-4 col-xs-12 inbox-panel">
                    <div> 
                        <a href="#" class="btn btn-custom btn-block waves-effect waves-light">Compose</a> 
                    </div>
                </div> --}}
                <div class="col-lg-10 col-md-9 col-sm-8 col-xs-12 mail_listing">
                    <h3 class="box-title">Compose Message</h3>
                    <button class="btn btn-outline btn-default waves-effect waves-light m-b-10" onclick="displayFilter()"> <i class="fa fa-search m-r-5"></i> <span>Filter</span></button>
                    <form method="post" action="notification" enctype="multipart/form-data">
                        <div id="filterDiv">
                            <div class="form-group">
                                <div class="row">
                                    <div class="col-sm-6">
                                        <label>By Sector</label>
                                        <select class="form-control select2" multiple data-style="form-control" name="sectors" style="width:100%;">
                                            {{# each sectors }}
                                            <option value="{{this}}">{{this}}</option>
                                            {{/each}}
                                        </select>
                                    </div>
                                    <div class="col-sm-6">
                                        <label>By Category</label>
                                        <select class="form-control select2" multiple name="category" style="width:100%;">
                                            {{# each category }}
                                            <option value="{{this}}">{{this}}</option>
                                            {{/each}}
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="To:" name="emailTo"> </div>
                        <div class="form-group">
                            <input class="form-control" placeholder="Subject:" name="subject"> </div>
                        <div class="form-group">
                            <textarea name="message" class="textarea_editor form-control" rows="15" placeholder="Enter text ..."></textarea>
                        </div>
                        {{!-- <h4><i class="ti-link"></i> Attachment</h4>
                        <div action="#" class="dropzone">
                            <div class="fallback">
                                <input name="file" type="file" multiple /> </div>
                        </div> --}}
                        <hr>
                        <button type="submit" class="btn btn-primary"><i class="fas fa-envelope"></i> Send</button>
                        <button class="btn btn-default" type="reset"><i class="fa fa-times"></i> Discard</button>
                    </form>
                    
                </div>
            </div>
        </div>
    </div>
</div>